# ── Core Django ───────────────────────────────────────────────────────────────
Django==5.2.4
asgiref==3.9.1
sqlparse==0.5.3

# ── ASGI / WebSocket ──────────────────────────────────────────────────────────
# channels + daphne handle HTTP + WebSocket in one process.
# Use Daphne (or uvicorn) — never plain `runserver` in production.
channels==4.3.1
channels_redis==4.3.0
daphne>=4.0,<5.0
# uvicorn: used by devserver management command
uvicorn[standard]>=0.30

# ── Database ──────────────────────────────────────────────────────────────────
psycopg[binary]>=3.1,<4
dj-database-url==1.0.0

# ── Cache / Channel layer ─────────────────────────────────────────────────────
redis==7.0.1
django-redis>=6.0

# ── Environment / Config ──────────────────────────────────────────────────────
django-environ==0.12.0
python-decouple==3.8
python-dotenv>=1.0.0

# ── Media / Storage ───────────────────────────────────────────────────────────
cloudinary==1.44.1
django-cloudinary-storage==0.3.0
pillow==11.3.0

# ── Static files ──────────────────────────────────────────────────────────────
whitenoise==6.5.0

# ── Background jobs ───────────────────────────────────────────────────────────
APScheduler==3.11.0
pytz==2025.2

# ── Web Push / VAPID ──────────────────────────────────────────────────────────
pywebpush==2.0.3
cffi==1.17.1
pycparser==2.22

# ── HTTP ──────────────────────────────────────────────────────────────────────
requests==2.32.4
certifi==2025.7.14
charset-normalizer==3.4.2
idna==3.10
urllib3==2.5.0

# ── Template helpers ──────────────────────────────────────────────────────────
django-widget-tweaks==1.5.0
django-htmx==1.23.2

# ── PDF generation ────────────────────────────────────────────────────────────
# WeasyPrint: HTML → PDF for follow-up report exports.
# Needs system libs on Linux: sudo apt install libpango-1.0-0 libpangoft2-1.0-0 libgdk-pixbuf2.0-0
weasyprint>=65.0
tinycss2==1.4.0
cssselect2==0.8.0
pydyf==0.11.0
pyphen==0.17.2
fonttools==4.59.0
tinyhtml5==2.0.0
webencodings==0.5.1
Brotli==1.1.0
zopfli==0.2.3.post1
# reportlab: attendance sheets, certificates
reportlab==4.4.3

# ── Excel / CSV ───────────────────────────────────────────────────────────────
openpyxl==3.1.5
et_xmlfile==2.0.0

# ── Audio metadata (music app) ────────────────────────────────────────────────
mutagen==1.47.0

# ── Geocoding (attendance geo-fencing) ────────────────────────────────────────
geopy==2.4.1

# ── HTML parsing ──────────────────────────────────────────────────────────────
beautifulsoup4==4.14.2

# ── Dev / debug ───────────────────────────────────────────────────────────────
django-debug-toolbar==6.0.0   # guard with if DEBUG in urls.py

# ── WSGI fallback ─────────────────────────────────────────────────────────────
gunicorn==20.1.0

# ── Misc / transitive ─────────────────────────────────────────────────────────
setuptools==80.9.0
six==1.17.0